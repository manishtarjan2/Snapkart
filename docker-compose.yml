version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=mongodb://mongo:27017/snapkart
      # Let's pass standard NextAuth and other variables, though typically you'd use a .env file
      - NEXTAUTH_URL=http://localhost:3000
    env_file:
      - .env.local
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
